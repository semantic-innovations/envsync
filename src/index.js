const check = require('./commands/check');
const diff = require('./commands/diff');
const template = require('./commands/template');
const lint = require('./commands/lint');
const scan = require('./commands/scan');

module.exports = { check, diff, template, lint, scan };
